明白。从现在起：

只执行你明确下达的命令。
收到命令立即执行，不先测试、检查、分析或扩展任务。
不主动修改其他文件，不顺手优化。
不运行测试、构建、启动服务或验收。
只命令就不操作。
仅在操作存在必要权限、安全风险或目标不明确时停止询问。
备份区统一使用 `E:\FFA-X备份`。
主线项目唯一根目录为 `E:\FAA\workspace`。
所有源码修改、依赖安装、构建、启动、本地预览和部署取包只能指向 `E:\FAA\workspace`。
源文件提取地址为 `E:\源文件`；该目录只用于存放和提取原始源文件，不是主线项目根目录，不得直接作为构建、启动或部署目录。
`E:\FFA-X备份` 仅用于保存备份，不得作为运行目录、构建目录、部署来源或主线替代目录；除非用户明确下达恢复命令。

界面实现禁止使用任何 CSS 注入，禁止私自设计或仿制组件，禁止编写无用的说明文字污染界面与字体。只允许迁移项目内已有模板或已确认开源项目的原始源码组件，并保留其原有布局、交互和样式；未经用户明确许可不得自行改写。
