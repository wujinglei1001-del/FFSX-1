明白。从现在起：

只执行你明确下达的命令。
收到命令立即执行，不先测试、检查、分析或扩展任务。
不主动修改其他文件，不顺手优化。
不运行测试、构建、启动服务或验收。
只命令就不操作。
仅在操作存在必要权限、安全风险或目标不明确时停止询问。
备份区统一使用 `E:\FFA-X备份`。
主线项目唯一根目录为 `E:\FAA\workspace`。
所有源码修改、依赖安装、构建、启动、本地预览和部署取包只能指向 `E:\FAA\workspace`。
源文件提取地址为 `E:\源文件`；该目录只用于存放和提取原始源文件，不是主线项目根目录，不得直接作为构建、启动或部署目录。
`E:\FFA-X备份` 仅用于保存备份，不得作为运行目录、构建目录、部署来源或主线替代目录；除非用户明确下达恢复命令。

界面实现禁止使用任何 CSS 注入，禁止私自设计或仿制组件，禁止编写无用的说明文字污染界面与字体。只允许迁移项目内已有模板或已确认开源项目的原始源码组件，并保留其原有布局、交互和样式；未经用户明确许可不得自行改写。

真实系统交付规则：
1. 不得用自编页面、静态卡片、演示壳或伪造数据代替真实功能。
2. 必须从系统现有模板中直接选取并迁移原始源码，保留原有页面层级、布局、卡片、按钮、边框、图标、字号、标题样式和交互逻辑。
3. 业务功能必须接入真实 API，查询、创建、修改、删除、审核、通知、权限等操作必须使用服务器端真实接口和真实数据。
4. 仅显示 API 名称、状态标签或空白表格，不算功能完成；必须验证实际请求、响应和操作闭环。
5. 前端和后端按实际改动部署到服务器；只更新界面时必须明确说明后端容器未重建，不得宣称已完成后端部署。
6. 每次正式部署必须保留可核验的版本号、部署输出、线上地址和必要的 API 请求证据。
7. 未经用户明确许可，不得重新设计 UI、添加自编文案、替换原始交互，或把真实功能降级为展示页面。
